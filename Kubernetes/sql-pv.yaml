apiVersion: v1
kind: PersistentVolume
metadata:
  name: sql-pv
spec:
  storageClassName: "local-path"
  capacity:
    storage: 512Mi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Recycle
  claimRef:
    name: sql-pvc
    namespace: eatfresh
  hostPath:
    path: "~/slet/"